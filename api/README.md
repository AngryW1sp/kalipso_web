# Отправка заявок в Telegram (Python)

## Настройка

1. **Создайте бота** в Telegram через [@BotFather](https://t.me/BotFather):
   - Команда `/newbot`
   - Укажите имя и username бота
   - Сохраните выданный **токен**

2. **Узнайте chat_id**:
   - Напишите боту `/start`
   - Откройте: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
   - В ответе найдите `"chat":{"id":123456789}` — это ваш chat_id

3. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```

4. **Запуск с переменными окружения**:
   ```bash
   set TELEGRAM_BOT_TOKEN=123456789:ABC...
   set TELEGRAM_CHAT_ID=123456789
   uvicorn server:app --host 0.0.0.0 --port 8000
   ```
   (На Linux/Mac: `export TELEGRAM_BOT_TOKEN=...` и `export TELEGRAM_CHAT_ID=...`)

5. Откройте: http://localhost:8000

## Деплой

На сервере задайте переменные окружения и запустите через systemd, gunicorn/uvicorn или другой процесс-менеджер. Можно использовать `.env` и пакет `python-dotenv` для загрузки переменных.

## Формат уведомления

```
🔔 Новая заявка с сайта KALIPSO

📞 Телефон: +7 (999) 123-45-67
📍 Страница: equipment-catalog.html
```
